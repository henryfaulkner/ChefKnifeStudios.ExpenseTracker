<div class="search-blade">
    <BladeContainer @ref="_bladeContainer">
        <ContentFragment>
            <MatH4 class="clr-on-1t-container">
                Search Results
            </MatH4>

            <MatBody2>@SearchViewModel.SearchResult.RagMessage</MatBody2>

            <MatList Id="result-list" Class="result-list">
                @{
                    int i = 0;
                }
                @foreach (var expense in SearchViewModel.SearchResult.Expenses)
                {
                    i += 1;
                    int ii = i;
                    <MatListItem Class="list-item">
                        <div>
                            <MatBody1 Style="margin: 0; white-space: nowrap;">@expense.Name</MatBody1>
                            <MatBody2 Class="clr-1s" Style="margin: 0; white-space: nowrap;">@expense.Cost.ToString("C2")</MatBody2>
                        </div>
                        <MatBody1 Style="margin: 0; white-space: nowrap;">@(expense.Budget?.Name ?? "N/A")</MatBody1>
                    </MatListItem>
                    @if (ii != SearchViewModel.SearchResult.Expenses.Count())
                    {
                        <MatListDivider></MatListDivider>
                    }
                }
            </MatList>
        </ContentFragment>
    </BladeContainer>
</div>