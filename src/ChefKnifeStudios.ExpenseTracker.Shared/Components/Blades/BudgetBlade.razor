@using MatBlazor

<div class="budget-blade">
    <BladeContainer @ref="_bladeContainer">
        <ContentFragment>
            <MatH4 class="clr-on-1t-container">
                Budget
            </MatH4>

            <form>
                <MatTextField 
                    @bind-Value="@_name"
                    Label="Name"
                    Icon="create"
                    Required="true" 
                />
                <MatDatePicker 
                    @bind-Value="_startDate" 
                    Label="Start Date" 
                    Format="MMMM dd, yyyy" 
                />
                <MatDatePicker 
                    @bind-Value="_endDate" 
                    Label="End Date" 
                    Format="MMMM dd, yyyy" 
                />
                <MatTextField 
                    @bind-Value="@_budget" 
                    Label="Budget" 
                    Icon="attach_money" 
                    Required="true" 
                />

                <MatButton 
                    OnClick="HandleSubmitPressed"
                    Unelevated="true"
                >
                    SUBMIT
                </MatButton>
            </form>
        </ContentFragment>
    </BladeContainer>
</div>